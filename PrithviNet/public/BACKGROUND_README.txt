Background pattern
-----------------
To show the decorative mandala pattern with the gradient, place your pattern image here as:

  background-pattern.png

The app uses it as a layer under the soft blue/mint/lavender gradient so both the colour and the pattern are visible.
